[project]
name = "gfetch"
version = "0.2.0"
description = "A simple and modern tool for searching and downloading genome data from NCBI Datasets."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.28.0",
]

[project.scripts]
gfetch = "cli:main"
[project]
name = "gfetch"
version = "0.2.0"
description = "Genome data search and download tool powered by NCBI Datasets"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.28.0",
    "datasets>=18.22.1",
]

[project.scripts]
gfetch = "cli:main"